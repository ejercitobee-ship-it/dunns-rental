// Placeholder for Task 10. Renders a heading only, so the /portal route
// compiles and the shell (PortalLayout) can be seen and reviewed now.
export function TenantHome() {
  return <h1 className="font-display text-2xl text-ink">Home</h1>;
}
