// Placeholder for Task 10. Renders a heading only, so the /portal/information
// route compiles and the shell (PortalLayout) can be seen and reviewed now.
export function TenantInfo() {
  return <h1 className="font-display text-2xl text-ink">My information</h1>;
}
