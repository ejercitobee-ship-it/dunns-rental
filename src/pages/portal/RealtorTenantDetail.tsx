// Placeholder for Task 11. Renders a heading only, so the
// /portal/tenants/:id route compiles and the shell (PortalLayout) can be
// seen and reviewed now.
export function RealtorTenantDetail() {
  return <h1 className="font-display text-2xl text-ink">Tenant</h1>;
}
