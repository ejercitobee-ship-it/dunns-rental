// Placeholder for Task 11. Renders a heading only, so the /portal route
// compiles and the shell (PortalLayout) can be seen and reviewed now.
export function RealtorTenants() {
  return <h1 className="font-display text-2xl text-ink">My tenants</h1>;
}
